export interface AppConfig {
  appName: string;
  companyName: string;
  pageHeader: string;
  pageSubheader: string;
  theme: {
    primaryColor: string;
    backgroundColor: string;
    fontFamily: string;
  };
}
